<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_vault">

    <fragment
        android:id="@+id/nav_vault"
        android:name="com.encrypto.android.geoencrypt.ui.home.HomeFragment"
        android:label="@string/menu_vault"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_HomeFragment_to_HomeSecondFragment"
            app:destination="@id/nav_vault" />
    </fragment>
    <fragment
        android:id="@+id/nav_termsAndConditions"
        android:name="com.encrypto.android.geoencrypt.ui.termsandconditions.tandcFragment"
        android:label="@string/menu_termsAndC"
        tools:layout="@layout/fragment_gallery" />

</navigation>